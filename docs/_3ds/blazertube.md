---
author: idkwh, nebulagamez
categories:
- revival
description: Native YouTube 3DS app revival
layout: app
llm_generation: unknown
stars: 0
systems:
- 3DS
title: BlazerTube
updated: '2026-08-07T14:09:00Z'
version: v1.801
---
### Installation instructions

<div class="alert alert-info">These installation instructions have been automatically generated based on Universal-Updater's installation scripts</div>
<details class="alert alert-secondary"><summary>Install... (EUR)</summary>
<ol>
<li>Be sure to enable game patching in Luma3DS (hold SELECT while booting your console, then enable "Enable Game Patching").</li>
<li>You will need Pretendo Network installed and enabled to use this (via Nimbus).</li>
<li>You will need a copy of YouTube v2080 installed.</li>
<li>Videos play smoothly on New 3DS consoles only. Do you wish to proceed?</li>
<li>The service is in public beta, it may not work as expected.</li>
<li>Create the folder <code>sdmc:/luma/titles/00040000000CCD00/</code></li>
<li>Download <code>code.ips</code> to <code>sdmc:/luma/titles/00040000000CCD00/code.ips</code> on your SD card</li>
</ol>
</details>

<details class="alert alert-secondary"><summary>Install... (USA)</summary>
<ol>
<li>Be sure to enable game patching in Luma3DS (hold SELECT while booting your console, then enable "Enable Game Patching").</li>
<li>You will need Pretendo Network installed and enabled to use this (via Nimbus).</li>
<li>You will need a copy of YouTube v2096 installed.</li>
<li>Videos play smoothly on New 3DS consoles only. Do you wish to proceed?</li>
<li>The service is in public beta, it may not work as expected.</li>
<li>Create the folder <code>sdmc:/luma/titles/00040000000B0F00/</code></li>
<li>Download <code>code.ips</code> to <code>sdmc:/luma/titles/00040000000B0F00/code.ips</code> on your SD card</li>
</ol>
</details>

<details class="alert alert-secondary"><summary>Install... (JPN)</summary>
<ol>
<li>Be sure to enable game patching in Luma3DS (hold SELECT while booting your console, then enable "Enable Game Patching").</li>
<li>You will need Pretendo Network installed and enabled to use this (via Nimbus).</li>
<li>You will need a copy of YouTube v2096 installed.</li>
<li>Videos play smoothly on New 3DS consoles only. Do you wish to proceed?</li>
<li>The service is in public beta, it may not work as expected.</li>
<li>Create the folder <code>sdmc:/luma/titles/00040000000D3000/</code></li>
<li>Download <code>code.ips</code> to <code>sdmc:/luma/titles/00040000000D3000/code.ips</code> on your SD card</li>
</ol>
</details>

